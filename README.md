##*Information on the contents of the repo*

### UARTapp
This folder contains the code files for a PyQt-based UART application which can be used to communicate with a device via UART ( 8 data bits, 1 stop bit, 1 start bit) with custom baudrate. 
Download the zip file of the folder, extract it and run *app.py* to start the PyQt application.
